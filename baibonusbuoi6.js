let so = prompt("nhap so vao day")
let array =[so , so +so, so+so+so,so+so+so+so,so+so+so+so+so ,so+so+so+so+so+so, so+so+so+so+so+so+so, so+so+so+so+so+so+so+so,so+so+so+so+so+so+so+so+so ]
for(let i = 0 ; i < Number(so);i++){
console.log(array[i])
}
